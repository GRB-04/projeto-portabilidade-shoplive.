{
  "name": "shoplive-portability-tests",
  "version": "1.0.0",
  "description": "Testes automatizados de portabilidade para o projeto ShopLive",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
